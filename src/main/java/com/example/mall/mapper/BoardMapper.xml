<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.example.mall.mapper.BoardMapper">
	<!-- 우정: Board 저장 SQL 쿼리 -->
	<insert id="insertBoard" parameterType="com.example.mall.vo.Board">
		INSERT INTO board (orders_no, board_content, create_date, update_date)
		VALUES (#{ordersNo}, #{boardContent}, NOW(), NOW())
	</insert>

</mapper>
